%a{:href => "/plans/new"} 添加教学计划

- if @plans.any?
  %h1 现有教学计划
  %ul
    - @plans.each do |plan|
      %li
        %a{:href => "/plans/#{plan.id}"}= plan.name
        %a.remove{:href => "/plans/#{plan.id}"} [删除]
      
